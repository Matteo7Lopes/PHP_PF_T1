/:
  controller: Base
  action: index

/contact:
  controller: Base
  action: contact


/portfolio:
  controller: Base
  action: portfolio

/login:
  controller: Auth
  action: login

/logout:
  controller: Auth
  action: logout

/register:
  controller: Auth
  action: register

/validate:
  controller: Auth
  action: validate

/forgot-password:
  controller: Auth
  action: forgotPassword

/reset-password:
  controller: Auth
  action: resetPassword

/admin:
  controller: Admin
  action: dashboard

/test-db:
  controller: Base
  action: testDb